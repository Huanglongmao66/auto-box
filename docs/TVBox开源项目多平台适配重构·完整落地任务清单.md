# TVBox开源项目多平台适配重构·完整落地任务清单

## 前置准备阶段（基础环境 \& 架构铺垫）

**目标**：搭建KMP多平台工程架构，解耦原生安卓强依赖，统一项目规范，为全平台适配打底

- 初始化Kotlin Multiplatform多模块工程，严格按照既定目录结构创建分层目录（common/core、common/device\-api、common/utils、platform各平台、app各入口）

- 统一项目编译配置，搭建Gradle KMP打包基础环境，配置Java/Kotlin版本、混淆、依赖版本统一管理

- 梳理原版TVBox全部核心业务代码，标记安卓专属硬编码API、Activity、Context、ExoPlayer耦合代码，形成耦合问题清单

- 定义全局统一规范：命名规范、日志规范、错误码规范、资源加载规范、分支管理规范

- 抽离公共工具底座，迁移通用工具类（加密、正则、JSON、日期、线程调度）至common/utils，实现全平台无依赖复用

- 搭建基础CI编译脚本，支持分平台编译参数配置（android/desktop/web/linux）

## 第一阶段：核心抽象层重构（最关键，解耦跨平台依赖）

**目标**：完成设备、播放器、存储三大核心抽象，彻底剥离平台专属依赖

### 1\. 设备通用抽象层开发

- 完整定义DeviceApi统一抽象接口，包含存储、网络、弹窗、剪贴板、权限、系统能力等全部方法

- 完成Android平台DeviceApi实现，兼容原版TVBox全部系统能力

- 完成Desktop（Windows/macOS/Linux）平台DeviceApi适配实现

- 完成Web平台DeviceApi适配实现（LocalStorage、浏览器能力适配）

- 完成嵌入式Linux TV盒子DeviceApi轻量化适配实现

### 2\. 跨平台播放器抽象层开发

- 定义标准化IPlayer播放器接口及播放器监听回调协议

- 重构安卓端播放器，基于接口封装ExoPlayer/Media3，移除业务层直接依赖

- 开发桌面/Linux端MPV/VLC播放器适配实现

- 开发Web端HTML5\+hls\.js播放器适配实现

- 统一封装字幕解析、倍速、多音轨、seek、硬解码控制通用逻辑至common核心层

### 3\. 跨平台存储层重构

- 抽象统一StorageManager存储管理接口，规范缓存、用户数据、配置文件目录规则

- 接入跨平台SQLite数据库，迁移播放历史、收藏、订阅数据存储逻辑

- 替换安卓专属配置文件，改为通用JSON配置，实现全平台兼容读取

- 开发旧版安卓TVBox数据迁移工具，支持配置、历史、收藏一键迁移至多端

## 第二阶段：核心业务层全平台迁移

**目标**：100%复用TVBox原生核心业务，零功能阉割、兼容全部影视源规则

- 将安卓端数据源解析核心逻辑（JSON源、聚合、订阅、本地导入、嗅探解析）完整迁移至common/core

- 重构网络模块，替换安卓OkHttp为Ktor跨平台网络库，统一Cookie、代理、防盗链处理逻辑

- 适配多平台JS解析引擎，兼容网页嗅探、解析脚本，保证第三方源100%可用

- 迁移搜索、热搜、分类、资源筛选、播放队列、选集管理核心业务逻辑

- 重构缓存预加载、进度保存、视频信息解析逻辑，实现全平台统一

- 剥离所有业务层平台判断代码，业务层仅依赖抽象接口，不感知底层系统

## 第三阶段：多端UI适配重构（Compose Multiplatform统一UI）

**目标**：一套公共UI组件，自适应TV大屏、桌面、网页、移动端

- 将原有安卓Compose公共组件迁移至common通用UI层，剔除安卓专属控件

- 封装全局主题、换肤、字体、适配规则，实现全平台样式统一

- 开发双模式自适应布局：TV焦点遥控模式、键鼠/触摸通用模式

- 适配TV端遥控器上下左右焦点逻辑、选中状态、按键监听

- 完成桌面端窗口缩放、拖拽、大小自适应适配

- 完成Web端页面适配、全屏、鼠标交互、快捷键适配

- 各平台独有UI能力差异化实现（安卓通知、桌面弹窗、网页弹窗）

## 第四阶段：各平台专项适配开发

**目标**：补齐各平台差异化能力，适配不同终端运行特性

### 1\. Android端适配（完全兼容原版TVBox）

- 保留原版全部功能：U盘导入、本地源、后台播放、投屏、外部播放器唤起

- 适配Android TV、电视盒子、手机多设备分辨率、焦点逻辑

- 优化打包配置，输出arm64/x86多架构APK，支持混淆、签名打包

### 2\. Windows/macOS/Linux桌面端适配

- 适配桌面窗口生命周期、最小化、置顶、退出逻辑

- 内置MPV/VLC解码器，解决桌面端视频兼容、硬解码问题

- 适配桌面键鼠快捷键、窗口缩放、全屏播放

### 3\. Web端适配

- 适配浏览器跨域、本地存储、全屏权限、后台播放限制

- 优化HLS/MP4流媒体播放兼容性，适配主流浏览器

- 实现网页端本地JSON源导入、导出、持久化存储

### 4\. 嵌入式Linux TV盒子适配

- 轻量化编译，裁剪冗余功能，适配嵌入式ARM架构

- 适配盒子底层硬件解码、遥控器驱动、大屏显示

- 优化内存占用，适配低配置电视盒子设备

## 第五阶段：编译打包 \& 功能裁剪配置

**目标**：实现一键分平台打包，支持按需功能裁剪，精简包体积

- 编写跨平台一键打包脚本（shell/bat），支持单指令编译各平台安装包

- 配置编译参数开关：平台切换、播放器内核切换、投屏/更新等功能启停

- 实现各平台差异化打包优化（安卓精简资源、桌面端精简依赖、Web端压缩静态资源）

- 输出各平台成品：APK、EXE、DMG、DEB、网页静态包、Linux可执行文件

## 第六阶段：全维度测试 \& 兼容性校验

**目标**：保证全平台功能一致、兼容原版生态、无业务bug

- 兼容性测试：验证所有原版TVBox影视源、解析规则、嗅探脚本正常可用

- 功能全量回归：播放、选集、倍速、字幕、收藏、历史、搜索、订阅全功能测试

- 多设备适配测试：不同分辨率TV、桌面设备、浏览器、嵌入式盒子

- 性能测试：启动速度、内存占用、播放流畅度、缓存加载效率

- 异常测试：断网、视频失效、非法源、权限拒绝、闪退崩溃测试

- 数据迁移测试：旧版数据完整迁移，无丢失、无错乱

## 第七阶段：文档归档 \& 项目收尾

- 输出项目架构文档、模块说明、抽象接口API文档

- 编写各平台编译、部署、打包、使用教程

- 归档全套项目资产：代码、脚本、配置、测试报告、迁移工具

- 梳理已知问题、适配限制、后续迭代优化方向

## 可选拓展任务（基础版本完成后迭代）

- 开发多端数据云同步功能（收藏、播放进度跨设备互通）

- 适配华为/小米TVOS专项系统适配

- 开发Linux后台服务模式，支持远程网页控制

- 优化全平台GPU硬解码自动调度逻辑

- 开发轻量化命令行解析版本

> （注：部分内容可能由 AI 生成）
